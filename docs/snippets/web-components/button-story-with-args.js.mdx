```js
// Button.stories.js

import { Button } from './Button';

export default {
  title: 'Button',
  render: (args) => Button(args),
};

export const Primary = {
  args: {
    primary: true,
    label: 'Button',
  },
};
```
